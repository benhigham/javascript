---
'@benhigham/eslint-config': minor
---

`browser` config: prune three non-standard entries (`opera`, `defaultStatus`,
`defaultstatus`) from `no-restricted-globals` and enrich remaining entries
with explanatory messages.
